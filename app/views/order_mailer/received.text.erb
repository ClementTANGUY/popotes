
Bonjour <%= @cook.user.first_name %>,
===============================================

T'as reçu une commande !

<%= "#{@order.first_name} souhaite du rab : "%>

<%= "- #{@order_item.quantity} portion(s) de #{@order_item.dish.name}" %>

Pour remettre ce plat, tu as rendez-vous à ton adresse active :

<% @order_item.dish.cook.places.each do |place| %>
  <% if place.active %>
    <%= "#{place.full_address}, #{place.zip_code} #{place.city}" %>
  <% end %>
<% end %>

<%= "Le #{@order_item.dish.collect_date.strftime('%d/%m/%Y')} à #{@order_item.dish.collect_date.strftime('%H:%M')}" %>


Attention : sois à l'heure à ton lieu de rendez-vous.<br>
La confiance ainsi que la ponctualité réciproque des chefs et des utilisateurs rendent l’expérience Rab It unique...
On compte sur toi !

Ce plat te rapporte dans la poche : <%= number_to_currency(@order_item.net_for_cook, locale: :fr) %>
Après nos frais de service : <%= number_to_currency(@order_item.cook_charge, locale: :fr) %>

Merci et à très bientôt sur Rab It !
